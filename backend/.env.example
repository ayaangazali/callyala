# =============================================================================
# Call Yala - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values

# -----------------------------------------------------------------------------
# App Settings
# -----------------------------------------------------------------------------
APP_ENV=local                     # local, development, production
PORT=8000                         # Server port
MOCK_MODE=false                   # Set to true to use mock data (no API calls)
DEFAULT_TIMEZONE=America/New_York # Your timezone

# -----------------------------------------------------------------------------
# Google Sheets Integration
# -----------------------------------------------------------------------------
# 1. Create a Google Cloud project: https://console.cloud.google.com
# 2. Enable Google Sheets API
# 3. Create a Service Account and download the JSON key
# 4. Place the JSON file at ./secrets/service_account.json
# 5. Share your Google Sheet with the service account email
GOOGLE_SHEETS_SPREADSHEET_ID=    # The ID from your Google Sheet URL
GOOGLE_SHEETS_RANGE=Sheet1!A1:Z  # Range to read (or leave default for full sheet)
GOOGLE_SERVICE_ACCOUNT_JSON_PATH=./secrets/service_account.json

# -----------------------------------------------------------------------------
# ElevenLabs Voice AI
# -----------------------------------------------------------------------------
# 1. Sign up at https://elevenlabs.io
# 2. Get your API key from Settings
# 3. Create a Conversational AI agent and get the agent ID
# 4. (Optional) Set up a phone number for outbound calls
ELEVENLABS_API_KEY=              # Your ElevenLabs API key
ELEVENLABS_AGENT_ID=             # Your Conversational AI agent ID
ELEVENLABS_PHONE_NUMBER_ID=      # Phone number ID for outbound calls
ELEVENLABS_WEBHOOK_SECRET=       # Webhook secret for verifying callbacks

# -----------------------------------------------------------------------------
# Anthropic Claude AI
# -----------------------------------------------------------------------------
# 1. Sign up at https://console.anthropic.com
# 2. Create an API key
ANTHROPIC_API_KEY=               # Your Anthropic API key for Claude

# -----------------------------------------------------------------------------
# Optional: Advanced Settings
# -----------------------------------------------------------------------------
MAX_BATCH_SIZE=200               # Max calls per batch
HTTP_TIMEOUT_SECONDS=30          # API timeout
DATA_DIR=./data                  # Directory for local data storage

# -----------------------------------------------------------------------------
# CORS (comma-separated origins)
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000

